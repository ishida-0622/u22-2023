/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["k-ishida-u22-2023-mock.s3.ap-northeast-1.amazonaws.com"],
  },
};

module.exports = nextConfig;
